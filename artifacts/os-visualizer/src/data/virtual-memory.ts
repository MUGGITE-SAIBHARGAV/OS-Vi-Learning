import { TopicData } from '../types';

export const virtualMemoryData: Record<string, TopicData> = {
  'vm-intro': {
    id: 'vm-intro',
    title: 'Introduction to Virtual Memory',
    overview: "Virtual Memory is an OS technique that creates the illusion of a very large memory by combining physical RAM with disk storage. A program can be larger than the physical RAM available — the OS keeps only the actively-used parts in RAM and stores the rest on disk. From the program's perspective, it has access to a huge continuous address space, enabling programs larger than RAM to run, isolating processes from each other, and maximizing multiprogramming.",
    whyItExists: "RAM is limited and expensive. Without virtual memory, a program must fit entirely in RAM to run — severely limiting application size and the number of concurrent processes. Virtual memory removes this constraint: programs can be larger than RAM, multiple programs share RAM efficiently, and each process is isolated in its own virtual address space.",
    analogy: "Like reading a book in a library — you don't carry the entire library home (RAM is small). You work with one chapter at a time, and the librarian fetches the next chapter when needed (OS loads pages from disk). From your perspective, you have access to the whole library.",
    keyPoints: [
      { title: 'What is Virtual Memory?', description: "A memory management technique where the OS creates a virtual address space for each process that is larger than physical RAM. Only the portions currently needed are in RAM; the rest stays on disk." },
      { title: 'Why RAM Alone is Not Enough', description: "Modern programs can require gigabytes of memory. A system with 8 GB RAM cannot run a 16 GB program without virtual memory. Virtual memory solves this by using disk as an extension of RAM." },
      { title: 'Illusion of Large Memory', description: "Each process believes it has exclusive access to a large, contiguous address space. The OS and hardware (MMU) transparently map this virtual space to physical RAM frames and disk pages." },
      { title: 'Process Isolation', description: "Each process has its own virtual address space. Process A cannot access Process B's memory — even if they share physical RAM — because the MMU enforces separate translation tables per process." },
      { title: 'Pages and Frames', description: "Virtual memory is divided into fixed-size chunks called pages (typically 4 KB). Physical RAM is divided into frames of the same size. The OS maps virtual pages to physical frames using a page table." },
      { title: 'Benefits Summary', description: "Programs larger than RAM can run. More processes fit simultaneously. Processes are isolated. Shared libraries can be mapped into multiple processes without duplication. Memory-mapped files enable efficient I/O." },
    ],
    interviewNotes: [
      "What is virtual memory? A technique that creates the illusion of more RAM by using disk storage as an extension, enabling programs larger than physical RAM to run.",
      "Why is virtual memory needed? Programs can exceed RAM size; multiple processes need isolation; memory efficiency improves with demand paging.",
      "What is the difference between virtual and physical address? Virtual (logical) address is used by processes and generated by CPU. Physical address is the actual RAM location. The MMU translates between them.",
      "What is a page? A fixed-size block of virtual memory (typically 4 KB). The unit of transfer between RAM and disk.",
      "What is a page frame? A fixed-size block of physical RAM that holds one page.",
      "What is the page table? OS data structure mapping virtual page numbers to physical frame numbers for each process.",
      "What is demand paging? Pages are only loaded into RAM when they are actually accessed (on demand), not when the process starts.",
    ],
    quickRevision: [
      "Virtual memory = illusion of large RAM using RAM + Disk.",
      "Pages (virtual) map to Frames (physical) via Page Table.",
      "Page size typically 4 KB.",
      "Each process has its own virtual address space → isolation.",
      "Demand paging = load page only when needed.",
      "Page Fault = page not in RAM → OS loads it from disk.",
      "Thrashing = too many page faults → system slows to crawl.",
    ],
    quiz: [
      { id: 'vm-q1', question: 'What is the primary purpose of virtual memory?', options: ['To make RAM faster', 'To allow programs larger than physical RAM to run by using disk as an extension', 'To increase the number of CPU cores', 'To compress data in RAM'], correctIndex: 1, explanation: 'Virtual memory creates the illusion of a large address space by combining RAM with disk storage. Programs can be much larger than physical RAM — the OS loads only the needed pages into RAM on demand.' },
      { id: 'vm-q2', question: 'What is a virtual (logical) address?', options: ['The actual physical location in RAM', 'An address on the hard disk', 'The address generated by the CPU that is mapped to a physical address by the MMU', 'The address of the OS kernel'], correctIndex: 2, explanation: 'A virtual (logical) address is what the CPU generates and what programs use. The MMU translates it to a physical address in RAM at runtime.' },
      { id: 'vm-q3', question: 'What does the MMU do?', options: ['Manages disk I/O operations', 'Schedules CPU processes', 'Translates virtual (logical) addresses to physical addresses', 'Compresses memory pages'], correctIndex: 2, explanation: 'The MMU (Memory Management Unit) is hardware that translates every virtual address generated by the CPU into its corresponding physical address in RAM using the page table.' },
      { id: 'vm-q4', question: 'What is a page in the context of virtual memory?', options: ['A document stored on disk', 'A fixed-size block of virtual memory (typically 4 KB)', 'A physical RAM chip', 'A CPU register'], correctIndex: 1, explanation: 'A page is a fixed-size chunk of virtual memory (typically 4 KB). Pages are the unit of transfer between RAM and disk in virtual memory systems.' },
      { id: 'vm-q5', question: 'What is demand paging?', options: ['Loading all pages of a program into RAM at startup', 'Requesting more RAM from the OS', 'Loading pages into RAM only when they are actually accessed', 'Paging the CPU schedule'], correctIndex: 2, explanation: 'Demand paging loads pages into RAM only when the process actually tries to access them. Pages that are never accessed are never loaded — saving memory.' },
      { id: 'vm-q6', question: 'What is a page fault?', options: ['A bug in the program code', 'A CPU hardware failure', 'An event that occurs when the CPU accesses a virtual page not currently in physical RAM', 'Disk corruption'], correctIndex: 2, explanation: 'A page fault occurs when a process tries to access a virtual page that is not loaded in physical RAM. The OS handles it by finding the page on disk and loading it into a RAM frame.' },
      { id: 'vm-q7', question: 'What happens immediately after a page fault?', options: ['The process is terminated', 'The CPU continues executing the next instruction', 'A trap to the OS occurs, the OS loads the missing page from disk, then resumes the process', 'The system restarts'], correctIndex: 2, explanation: 'On page fault: (1) trap raised to OS, (2) OS finds page on disk, (3) loads into a free RAM frame, (4) updates page table, (5) resumes the faulting process.' },
      { id: 'vm-q8', question: 'What is thrashing?', options: ['A hardware failure in RAM', 'When a program has too many loops', 'A condition where the OS spends more time handling page faults than executing processes', 'Defragmenting virtual memory'], correctIndex: 2, explanation: 'Thrashing occurs when the working set of all active processes exceeds available RAM. Processes continuously swap pages in and out, and CPU spends most time on I/O rather than useful execution.' },
      { id: 'vm-q9', question: "What is the working set of a process?", options: ['All pages ever used by the process', 'The set of pages the process is actively using in a recent time window', 'The number of open files', 'The CPU time consumed'], correctIndex: 1, explanation: 'The working set is the collection of pages a process is actively referencing during a time window. If the working set fits in RAM, the process runs efficiently with few page faults.' },
      { id: 'vm-q10', question: 'How is a physical address computed from a virtual address using paging?', options: ['Physical Address = Virtual Address × Page Size', 'Physical Address = (Frame Number × Page Size) + Offset', 'Physical Address = Page Number + Offset', 'Physical Address = Virtual Address − Base Register'], correctIndex: 1, explanation: 'A virtual address is split into Page Number and Offset. The page table maps Page Number → Frame Number. Physical Address = (Frame Number × Page Size) + Offset.' },
      { id: 'vm-q11', question: 'What is the TLB (Translation Lookaside Buffer)?', options: ['A type of disk cache', 'A hardware cache that stores recent virtual-to-physical address translations to speed up memory access', 'A software data structure for page tables', 'A CPU register for arithmetic'], correctIndex: 1, explanation: 'The TLB is a fast hardware cache inside the MMU that stores recent page table lookups. TLB hit rates are typically >99% due to locality of reference.' },
      { id: 'vm-q12', question: 'Which page replacement algorithm replaces the page not used for the longest time in the future?', options: ['FIFO', 'LRU', 'Optimal (OPT)', 'Clock'], correctIndex: 2, explanation: "The Optimal (OPT) algorithm replaces the page whose next use is furthest in the future. It has the lowest possible page fault rate but cannot be implemented in practice." },
      { id: 'vm-q13', question: "What is Belady's Anomaly?", options: ['More RAM always reduces page faults', 'FIFO page replacement can cause MORE page faults when given MORE frames', 'LRU always performs better than FIFO', 'Page faults only occur on the first access'], correctIndex: 1, explanation: "Belady's Anomaly: with FIFO page replacement, giving a process MORE page frames can sometimes result in MORE page faults. LRU and OPT do not suffer from this." },
      { id: 'vm-q14', question: 'What causes thrashing?', options: ['Too much CPU time', 'Degree of multiprogramming exceeds available RAM — the combined working sets cannot fit in RAM', 'Running too few processes', 'Disk corruption'], correctIndex: 1, explanation: 'Thrashing is caused when the total working set memory of all active processes exceeds physical RAM. The OS constantly swaps pages; processes spend more time waiting for pages than executing.' },
      { id: 'vm-q15', question: 'What is the valid/invalid bit in a page table entry?', options: ['Indicates if the page is read-only', 'Indicates whether the page is currently loaded in RAM (valid) or is on disk (invalid)', 'Marks pages that belong to the OS', 'Tracks how many processes share the page'], correctIndex: 1, explanation: 'Each page table entry has a valid/invalid bit. Valid (1) means the page is in RAM. Invalid (0) means the page is not in RAM — accessing it triggers a page fault.' },
    ]
  },

  'logical-physical': {
    id: 'logical-physical',
    title: 'Logical vs Physical Address',
    overview: "Every memory access in a modern computer involves two addresses: the logical (virtual) address that the CPU and programs generate, and the physical address — the actual location in RAM. The CPU always uses logical addresses. The MMU transparently translates each logical address to its corresponding physical address before the memory access happens. This separation enables virtual memory, process isolation, and relocation.",
    whyItExists: "Without address separation, every program would need to know the exact RAM location it would run at — impossible in a multiprogrammed system where process locations change. Logical addresses let programs use stable addresses while the OS freely places processes anywhere in RAM.",
    analogy: "Like your home address (logical) vs GPS coordinates (physical). You give your address to people, the postal system (MMU) looks up your coordinates (physical) and routes the delivery. You don't need to know your GPS coordinates.",
    keyPoints: [
      { title: 'Logical Address', description: 'Generated by the CPU during program execution. Also called a virtual address. Programs are compiled using logical addresses (0, 1, 2...). The program never sees physical addresses.' },
      { title: 'Physical Address', description: 'The actual location in RAM hardware. Determined at runtime by the MMU. Physical addresses are invisible to user programs.' },
      { title: 'Address Translation', description: 'The MMU performs translation for every memory access: Logical Address → Page Table Lookup → Frame Number → Physical Address. This happens in hardware at CPU speed.' },
      { title: 'Page Number and Offset', description: 'Logical Address = Page Number | Offset. The page table maps Page Number → Frame Number. Physical Address = (Frame Number × Page Size) + Offset.' },
      { title: 'Address Space', description: 'Each process has its own virtual address space (e.g., 0 to 2^48 on modern systems). Two processes can have the same virtual address mapping to different physical frames.' },
      { title: 'Base and Limit (Simple Scheme)', description: 'In simple systems without paging, the MMU uses a base register (physical start) + limit register (size). Physical = Logical + Base. Paging extends this to fine-grained page-level translation.' },
    ],
    interviewNotes: [
      "What is the difference between logical and physical address? Logical = generated by CPU, used by programs. Physical = actual RAM location. MMU translates between them.",
      "What is address translation? The process of converting a logical address to a physical address via the page table and MMU.",
      "How is a physical address computed? Split logical into Page Number + Offset. Look up Frame Number in page table. Physical = Frame × PageSize + Offset.",
      "Why do processes use logical addresses? So they can be loaded anywhere in RAM without changing their code. Enables relocation and multiprogramming.",
      "What is an address space? The range of logical addresses a process can generate (0 to max virtual address).",
    ],
    quickRevision: [
      "Logical address = CPU-generated (program uses this).",
      "Physical address = actual RAM location.",
      "MMU translates logical → physical on every access.",
      "Logical = Page Number | Offset.",
      "Physical = (Frame Number × Page Size) + Offset.",
      "Each process has its own independent virtual address space.",
    ],
    quiz: []
  },

  'mmu-translation': {
    id: 'mmu-translation',
    title: 'MMU & Address Translation Simulator',
    overview: "The Memory Management Unit (MMU) is hardware that automatically translates every logical address generated by the CPU into a physical address. It splits the logical address into a page number and offset, looks up the frame number in the page table, then combines frame number + offset to produce the physical address. A TLB (Translation Lookaside Buffer) caches recent translations for speed.",
    whyItExists: "Address translation must happen on every single memory access — millions per second. Doing this in software would make programs thousands of times slower. The MMU performs translation in hardware at CPU speed, making virtual memory practical.",
    analogy: "The MMU is like an automatic GPS translator built into your car. You say 'go to 123 Virtual Street' and the GPS instantly looks up the real coordinates — without you ever needing to know the physical location.",
    keyPoints: [
      { title: 'MMU Location', description: 'The MMU sits between the CPU and RAM. Every memory read/write passes through the MMU, which performs address translation before the request reaches RAM.' },
      { title: 'Page Table', description: 'Each process has a page table (in RAM) mapping virtual page numbers to physical frame numbers. The MMU uses this table for translation.' },
      { title: 'TLB (Translation Lookaside Buffer)', description: 'A fast hardware cache inside the MMU storing recent virtual→physical translations. TLB hit = very fast. TLB miss = page table walk. Hit rates are typically 99%+ due to locality.' },
      { title: 'Translation Steps', description: '1. CPU generates logical address. 2. Split into Page Number + Offset. 3. Check TLB. 4. If miss: walk page table. 5. Combine Frame Number + Offset → Physical Address. 6. Access RAM.' },
      { title: 'Valid/Invalid Bit', description: 'Each page table entry has a valid bit. Valid = page in RAM. Invalid = page on disk → page fault raised.' },
      { title: 'PTBR', description: 'The OS stores the base address of the current process page table in the Page Table Base Register (PTBR). On context switch, PTBR is updated.' },
    ],
    interviewNotes: [
      "What is the MMU? Hardware that translates every virtual address to physical address in real-time.",
      "What is the TLB? A hardware cache of recent page table translations. Avoids slow page table walks.",
      "What is a TLB miss? The virtual address was not cached in TLB — MMU must do a full page table walk.",
      "Effective Access Time = (hit_rate × TLB_access) + ((1 − hit_rate) × (TLB + memory)) + memory_access.",
      "What is PTBR? Page Table Base Register — points to current process's page table in RAM.",
      "What happens on context switch? PTBR updated to new process page table. TLB flushed (or tagged with ASID).",
    ],
    quickRevision: [
      "MMU = hardware between CPU and RAM that translates addresses.",
      "TLB = hardware cache of recent translations (99%+ hit rate).",
      "TLB hit = 1 memory access. TLB miss = 2+ memory accesses.",
      "PTBR = points to current process page table.",
      "Context switch → PTBR updated + TLB flushed.",
      "Valid bit in PTE: 1=in RAM, 0=on disk (page fault).",
    ],
    quiz: []
  },

  'demand-paging': {
    id: 'demand-paging',
    title: 'Demand Paging',
    overview: "Demand paging is the strategy of loading pages into RAM only when they are actually accessed, not when the process starts. A process begins with NO pages in RAM. When it accesses a page not in RAM, a page fault occurs and the OS loads just that page. This lazy loading keeps RAM usage minimal — pages that are never accessed never consume RAM. It enables fast process startup and efficient memory use.",
    whyItExists: "Loading an entire program into RAM before it starts is wasteful — many parts may never execute (error handlers, rarely-used features). Demand paging loads only what is actually needed, maximizing RAM utilization across many processes.",
    analogy: "Like Netflix streaming — instead of downloading the entire movie before watching (loading all pages), you stream only the part you're watching right now (demand paging). Jump to a different scene → loads that part on demand.",
    keyPoints: [
      { title: 'Lazy Loading', description: "When a process starts, its pages are marked invalid. The first access to each page triggers a page fault that loads it. Pages that are never accessed are never loaded." },
      { title: 'Page Fault Handling', description: "On page fault: (1) OS traps. (2) Finds page on disk. (3) Finds or creates a free RAM frame (may need to evict another page). (4) Loads the page. (5) Updates page table. (6) Resumes process." },
      { title: 'Page Replacement', description: "When RAM is full and a new page must be loaded, the OS must evict an existing page. Algorithms: FIFO (oldest page), LRU (least recently used), Optimal (furthest future use)." },
      { title: 'Pure vs Pre-paging', description: "Pure demand paging = never load a page until it is accessed. Pre-paging = load some predicted pages in advance. Modern OSes use pre-paging at startup to reduce initial faults." },
      { title: 'Page Fault Rate Impact', description: "EAT = (1−p) × mem_time + p × disk_time. Since disk >> RAM, even 0.1% fault rate dramatically slows the system." },
      { title: 'Copy-on-Write (COW)', description: "When a process forks, the child shares the parent's pages. A page is only copied when either process modifies it — making fork() extremely fast." },
    ],
    interviewNotes: [
      "What is demand paging? Pages loaded into RAM only when accessed — lazy loading.",
      "What triggers a page fault? Accessing a virtual page not currently in RAM (valid bit = 0).",
      "What is page replacement? When RAM is full, evicting a page to make room for a new one.",
      "What is the optimal page replacement algorithm? Replace the page whose next use is furthest in the future (theoretical, not implementable).",
      "What is LRU page replacement? Replace the page that was least recently used.",
      "What is copy-on-write? Fork shares pages between parent and child until a write occurs — then a private copy is made.",
    ],
    quickRevision: [
      "Demand paging = load pages only on access (lazy).",
      "Page fault = page not in RAM → OS loads from disk.",
      "Page fault handling: trap → find page → load → update PTE → resume.",
      "FIFO replacement: evict oldest loaded page.",
      "LRU replacement: evict least recently used page.",
      "OPT replacement: evict page not needed longest (theoretical best).",
      "EAT = (1−p) × mem_time + p × disk_time.",
    ],
    quiz: []
  },

  'page-fault': {
    id: 'page-fault',
    title: 'Page Fault Simulator',
    overview: "A page fault is an interrupt raised when a process accesses a virtual page not currently in physical RAM. The OS handles it by loading the needed page from disk. Page replacement algorithms determine which existing RAM page gets evicted when RAM is full. FIFO evicts the oldest page, LRU evicts the least recently used, and the Optimal algorithm evicts the page whose next reference is furthest in the future.",
    whyItExists: "Page faults are the core mechanism that makes demand paging work. Understanding how they are handled and how replacement algorithms differ is fundamental to OS design and performance tuning.",
    analogy: "Like a librarian managing a small reading desk. When you request a book not on the desk (page fault), the librarian fetches it from the stacks (disk). If the desk is full, they must return another book first (page replacement).",
    keyPoints: [
      { title: 'Page Fault Steps', description: "1. CPU accesses virtual address. 2. MMU checks PTE — invalid. 3. Page fault trap. 4. OS saves state. 5. OS finds page on disk. 6. If RAM full: page replacement. 7. Load page. 8. Update PTE. 9. Resume process." },
      { title: 'FIFO Algorithm', description: "Replace the page in RAM the longest. Simple (queue). Suffers from Belady's Anomaly — more frames can cause more faults." },
      { title: 'LRU Algorithm', description: "Replace the page least recently used. Based on temporal locality. Performs close to optimal. Expensive to implement — approximated by Clock algorithm." },
      { title: 'Optimal (OPT) Algorithm', description: "Replace the page whose next reference is furthest in the future. Lowest possible fault rate. Cannot be implemented — used as a benchmark." },
      { title: "Belady's Anomaly", description: "Counter-intuitive: with FIFO, giving MORE frames can result in MORE page faults. LRU and OPT are stack algorithms and do not suffer from this." },
      { title: 'Clock Algorithm', description: "Practical LRU approximation. Pages in a circular list with a reference bit. On replacement, skip pages with bit=1 (clear it); replace first page with bit=0." },
    ],
    interviewNotes: [
      "What is a page fault? An interrupt when a process accesses a virtual page not in RAM.",
      "Describe FIFO page replacement. Replace the oldest page in RAM. Simple but suffers Belady's anomaly.",
      "Describe LRU. Replace the page not used for the longest time. Near-optimal, approximated by Clock.",
      "Describe Optimal algorithm. Replace the page with the furthest future reference. Theoretical best.",
      "What is Belady's Anomaly? More frames → more page faults with FIFO.",
      "What is the Clock algorithm? Efficient LRU approximation using circular list with reference bits.",
    ],
    quickRevision: [
      "Page fault = page not in RAM → trap → OS loads from disk.",
      "FIFO = evict oldest. Simple. Belady's anomaly possible.",
      "LRU = evict least recently used. Near-optimal.",
      "OPT = evict furthest future. Best possible, theoretical.",
      "Clock = practical LRU approximation using reference bit.",
      "Belady's Anomaly: FIFO only. More frames → MORE faults.",
    ],
    quiz: []
  },

  'thrashing': {
    id: 'thrashing',
    title: 'Thrashing',
    overview: "Thrashing is a severe performance collapse that occurs when a computer spends more time paging (swapping pages between RAM and disk) than executing actual process instructions. It happens when the total memory demand of all running processes far exceeds available RAM. The system becomes essentially unusable — CPU utilization drops near 0% while disk I/O maxes out at 100%.",
    whyItExists: "Thrashing is what happens when an OS tries to multiprogramming too aggressively without enough RAM. Understanding thrashing motivates the working set model, page fault frequency algorithms, and proper memory capacity planning.",
    analogy: "A chef (CPU) trying to cook 20 dishes simultaneously in a kitchen with 2 burners (RAM). They constantly move dishes on and off the stove (page faults), spending all their time rearranging rather than actually cooking.",
    keyPoints: [
      { title: 'Cause of Thrashing', description: "Thrashing occurs when the OS increases the degree of multiprogramming beyond available RAM. Each process needs more pages than available, so they constantly steal frames from each other." },
      { title: 'CPU Utilization Collapse', description: "As thrashing begins, CPU utilization drops toward 0%. The OS sees low CPU and (incorrectly) adds more processes — making thrashing worse. A vicious cycle." },
      { title: 'Working Set Model', description: "W(t,Δ) = pages referenced in last Δ time units. If sum of all working sets exceeds RAM, thrashing is inevitable. OS should suspend processes until working sets fit." },
      { title: 'Page Fault Frequency (PFF)', description: "Monitor each process's page fault rate. Too high → give more frames. Too low → take frames away. Can't give more → suspend the process." },
      { title: 'Prevention', description: "(1) Working set model. (2) PFF algorithm. (3) Add more RAM. (4) Reduce the number of running processes." },
      { title: 'Thrashing vs Swapping', description: "Swapping removes entire processes to free RAM. Thrashing is page-level — processes compete for frames without suspension. Swapping is the OS's drastic last resort." },
    ],
    interviewNotes: [
      "What is thrashing? When a system spends more time paging than executing useful work, causing near-zero CPU utilization.",
      "What causes thrashing? Too many processes in RAM → combined working sets exceed available RAM → constant page faults.",
      "How does the OS detect thrashing? CPU utilization drops dramatically while disk I/O spikes.",
      "How is thrashing prevented? Working set model: ensure each process's working set fits in RAM before running it.",
      "What is the working set model? Track pages each process actively uses. Total working sets must fit in RAM.",
      "What is PFF? Page Fault Frequency — dynamically adjusts frames based on each process's fault rate.",
    ],
    quickRevision: [
      "Thrashing = more time paging than executing.",
      "Cause: total working sets > available RAM.",
      "Effect: CPU ≈ 0%, Disk I/O ≈ 100%.",
      "OS misreads low CPU as underload → adds more processes → worse.",
      "Fix: working set model, PFF, suspend some processes, add RAM.",
      "Working set = pages actively used in last Δ time window.",
    ],
    quiz: []
  },

  'working-set': {
    id: 'working-set',
    title: 'Working Set & VM Playground',
    overview: "The Working Set Model is a framework for preventing thrashing by tracking which pages each process is actively using. The working set W(t, Δ) consists of all pages referenced in the last Δ time units. Only if a process's working set fits in RAM should it be allowed to run. This directly controls the degree of multiprogramming and keeps the system from thrashing.",
    whyItExists: "Thrashing occurs when processes are given too few frames. The working set model provides a principled way to determine exactly how many frames each process needs at any moment, enabling the OS to make informed decisions about which processes to run simultaneously.",
    analogy: "A student studying for exams (working set = current study materials on the desk). They only need the books relevant to today's topic — not their entire collection. The OS keeps each process's current working set in RAM and archives the rest.",
    keyPoints: [
      { title: 'Working Set Definition', description: "W(t, Δ) = set of distinct pages accessed in the time interval (t−Δ, t). The parameter Δ (delta) is the window size. Pages in the working set are 'active' and should stay in RAM." },
      { title: 'Working Set Size', description: "The working set size changes over time as the process enters different execution phases. Phases with high locality have small working sets; transitions between phases expand it temporarily." },
      { title: 'Policy', description: "If sum of all working set sizes ≤ available frames: run all processes. If sum > frames: suspend processes until it fits." },
      { title: 'Window Size Δ', description: "Δ too small: misses active pages → too many faults. Δ too large: includes inactive pages → wastes frames. Δ must balance accuracy and responsiveness." },
      { title: 'Page Fault Frequency', description: "Alternative to working sets: set upper/lower bounds on fault rate per process. Rate too high → more frames. Rate too low → reclaim frames." },
      { title: 'Locality of Reference', description: "Programs tend to access the same set of pages repeatedly (temporal locality) and pages near recently accessed ones (spatial locality). This is why the working set model works in practice." },
    ],
    interviewNotes: [
      "What is the working set? W(t, Δ) = pages referenced in the last Δ time units by a process.",
      "How does the working set prevent thrashing? Only run processes whose working sets fit in available RAM.",
      "What is locality of reference? Programs tend to access the same small set of pages repeatedly.",
      "What is the working set policy? If total working set size > available frames → suspend processes until it fits.",
      "What is PFF? Page Fault Frequency — dynamically adjusts frame allocation based on measured fault rate.",
    ],
    quickRevision: [
      "Working set W(t,Δ) = pages used in last Δ time window.",
      "If ΣWorking Sets > RAM → suspend processes → prevent thrashing.",
      "Small Δ = miss active pages. Large Δ = include stale pages.",
      "Locality of reference = programs reuse same pages → small working set.",
      "PFF: too many faults → more frames. Too few → take frames back.",
    ],
    quiz: []
  },
};
